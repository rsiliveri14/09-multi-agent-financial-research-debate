apiVersion: apps/v1
kind: Deployment
metadata:
  name: debate-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: debate-redis
  template:
    metadata:
      labels:
        app: debate-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: debate-redis
spec:
  selector:
    app: debate-redis
  ports:
    - port: 6379
      targetPort: 6379
